{
  "name": "chi-cto",
  "version": "0.1.0",
  "description": "Chi CTO - Autonomous Feature Orchestrator",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest",
    "build": "tsc && esbuild src/mcp.ts --bundle --platform=node --format=esm --outfile=dist/mcp.js",
    "dev": "ts-node src/index.ts",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "keywords": ["priority-scoring", "task-management", "orchestration"],
  "author": "Roderic Andrews",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
